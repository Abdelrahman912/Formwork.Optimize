<UserControl x:Class="FormworkOptimize.App.UI.Views.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FormworkOptimize.App.UI.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/FormworkOptimize.App;component/UI/Resources/MainResourceDictionary.xaml"/>
    </UserControl.Resources>
    <Grid >
        <Border Grid.Row="1" Background="#fff3cd" BorderBrush="#bfa65b" 
                BorderThickness="1" Margin="10 0 10 0" CornerRadius="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="InfoCircleOutline" Foreground="#543f03" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding}" Foreground="#543f03" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
            </StackPanel>
           
        </Border>
    </Grid>
</UserControl>
